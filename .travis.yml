language: java
cache:
  directories:
  - $HOME/.m2
jdk:
  - openjdk8
  - openjdk11
  - openjdk13
env:
  - PROFILES=javac
  - PROFILES=jdt
  - PROFILES=javac,el3
  - PROFILES=jdt,el3
  - PROFILES=javac,java11
  - PROFILES=jdt,java11
  - PROFILES=javac,el3,java11
  - PROFILES=jdt,el3,java11

matrix:
  exclude:

    - jdk: openjdk8
      env: PROFILES=javac,java11
    - jdk: openjdk8
      env: PROFILES=jdt,java11
    - jdk: openjdk8
      env: PROFILES=javac,el3,java11
    - jdk: openjdk8
      env: PROFILES=jdt,el3,java11
install: "mvn help:active-profiles help:effective-pom help:effective-settings -q -P ${PROFILES},-standard-with-extra-repos"
script: "mvn clean install -U -q -P ${PROFILES},-standard-with-extra-repos -Dmaven.javadoc.skip=true"
